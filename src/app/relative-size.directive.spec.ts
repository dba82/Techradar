import { RelativeSizeDirective } from './relative-size.directive';

describe('RelativeSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new RelativeSizeDirective();
    expect(directive).toBeTruthy();
  });
});
